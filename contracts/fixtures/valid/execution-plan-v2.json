{
  "schema": "openthrottle.execution-plan/v2",
  "pipeline_id": "core/structured",
  "plan_id": "stage-c-contracts",
  "units": [
    {
      "id": "contracts",
      "title": "Freeze contracts",
      "depends_on": [],
      "objective": "Freeze the closed public execution-plan schemas so downstream packages compile against a stable contract.",
      "requirements": [
        "Add a versioned successor schema whose units carry every applicable value directly, with no ID-indirection into a separate map.",
        "Reject unknown fields on every object in the contract."
      ],
      "files": [
        "contracts/src/execution-plan-v2.ts",
        "contracts/src/index.ts"
      ],
      "approach": [
        "Define ExecutionPlanUnitV2 and ExecutionPlanContractV2 as the canonical self-contained execution-plan types.",
        "Export parse and validate entry points from the package barrel."
      ],
      "tests": [
        "A valid fixture parses and normalizes to a stable digest.",
        "A malformed plan with a missing required field is rejected and names the unit and field."
      ],
      "acceptance": [
        "The contracts package exports parseExecutionPlanContractV2 and validateExecutionPlanContractV2.",
        "Every behavior-bearing unit carries objective, requirements, files, approach, tests, acceptance, and verification directly."
      ],
      "verification": [
        "Run the contracts package test suite and confirm the execution-plan contract tests pass."
      ]
    },
    {
      "id": "validation",
      "title": "Verify validation",
      "depends_on": ["contracts"],
      "objective": "Exercise the v2 contract's structural validation deterministically with focused contract tests.",
      "requirements": [
        "Cover duplicate unit IDs, dependency cycles, unresolved dependency references, invalid command names, unknown fields, and a missing required unit field."
      ],
      "files": [
        "contracts/fixtures/valid/execution-plan-v2.json",
        "contracts/src/execution-plan-v2.test.ts"
      ],
      "approach": [
        "Cover v2-specific validation boundaries directly in the contracts test suite."
      ],
      "tests": [
        "Invalid plans reject with stable diagnostic paths naming the offending field."
      ],
      "acceptance": [
        "Contract validation rejects malformed v2 plans with stable diagnostic paths naming the offending field."
      ],
      "verification": [
        "Run the contracts package test suite and confirm the v2 validation boundaries pass."
      ]
    }
  ],
  "commands": [
    { "name": "docs-check", "unit": null },
    { "name": "build", "unit": "contracts" }
  ]
}
