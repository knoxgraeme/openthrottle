# Pipeline core/tune@1

<!-- Generated by `npm run docs:pipelines --prefix supervisor` from supervisor/pipelines/.
     Do not edit by hand; edit the manifest and regenerate. CI fails the
     "Verify pipeline docs are regenerated" step when this page drifts. -->

- **Pipeline:** `core/tune@1`
- **Entry stage:** `analysis`
- **Max attempts:** 200
- **Max repair rounds:** 5

Immutable self-tuning pipeline with read-only corpus analysis and proposal generation, supervisor citation and differential-ratchet gates, fresh isolated mutation, command gates, semantic review and repair, exact-subject publication, provider verification, and mandatory human merge.

## Flow

```mermaid
flowchart TD
    %% core/tune@1
    analysis["analysis<br/>agent"]
    proposal["proposal<br/>agent"]
    citation_gate["citation_gate<br/>agent"]
    differential_ratchet["differential_ratchet<br/>agent"]
    isolated_edit["isolated_edit<br/>agent"]
    repair_edit["repair_edit<br/>agent"]
    test[/"test<br/>command gate"/]
    build[/"build<br/>command gate"/]
    semantic_review["semantic_review<br/>agent"]
    publish["publish<br/>agent"]
    provider{{"provider<br/>provider wait"}}
    terminal_shipped(["shipped"])
    terminal_no_change(["no_change"])
    terminal_needs_human(["needs_human"])
    terminal_failed(["failed"])
    analysis -->|"success"| proposal
    analysis ==>|"no_change"| terminal_no_change
    analysis ==>|"semantic_repair_required"| terminal_needs_human
    analysis -.->|"retryable_infrastructure_failure (≤2 → failed)"| analysis
    analysis ==>|"failure"| terminal_failed
    proposal -->|"success"| citation_gate
    proposal ==>|"no_change"| terminal_no_change
    proposal ==>|"semantic_repair_required"| terminal_needs_human
    proposal -.->|"retryable_infrastructure_failure (≤2 → failed)"| analysis
    proposal ==>|"failure"| terminal_failed
    citation_gate -->|"success"| differential_ratchet
    citation_gate ==>|"no_change"| terminal_no_change
    citation_gate ==>|"semantic_repair_required"| terminal_needs_human
    citation_gate -.->|"retryable_infrastructure_failure (≤2 → failed)"| analysis
    citation_gate ==>|"failure"| terminal_failed
    differential_ratchet -->|"success"| isolated_edit
    differential_ratchet ==>|"no_change"| terminal_no_change
    differential_ratchet ==>|"semantic_repair_required"| terminal_needs_human
    differential_ratchet -.->|"retryable_infrastructure_failure (≤2 → failed)"| analysis
    differential_ratchet ==>|"failure"| terminal_failed
    isolated_edit -->|"success"| test
    isolated_edit ==>|"no_change"| terminal_no_change
    isolated_edit -.->|"semantic_repair_required (≤5 → needs_human)"| isolated_edit
    isolated_edit -.->|"retryable_infrastructure_failure (≤3 → failed)"| isolated_edit
    isolated_edit ==>|"failure"| terminal_failed
    repair_edit -->|"success"| test
    repair_edit -->|"no_change"| test
    repair_edit -.->|"semantic_repair_required (≤5 → needs_human)"| repair_edit
    repair_edit -.->|"retryable_infrastructure_failure (≤3 → failed)"| repair_edit
    repair_edit ==>|"failure"| terminal_failed
    test -->|"success"| build
    test -->|"no_change"| build
    test ==>|"semantic_repair_required"| terminal_needs_human
    test -.->|"retryable_infrastructure_failure (≤2 → failed)"| analysis
    test -.->|"failure (≤5 → needs_human)"| repair_edit
    build -->|"success"| semantic_review
    build -->|"no_change"| semantic_review
    build ==>|"semantic_repair_required"| terminal_needs_human
    build -.->|"retryable_infrastructure_failure (≤2 → failed)"| analysis
    build -.->|"failure (≤5 → needs_human)"| repair_edit
    semantic_review -->|"success"| publish
    semantic_review -->|"no_change"| publish
    semantic_review -.->|"semantic_repair_required (≤5 → needs_human)"| repair_edit
    semantic_review -.->|"retryable_infrastructure_failure (≤2 → failed)"| analysis
    semantic_review ==>|"failure"| terminal_failed
    publish -->|"success"| provider
    publish ==>|"no_change"| terminal_no_change
    publish ==>|"semantic_repair_required"| terminal_needs_human
    publish -.->|"retryable_infrastructure_failure (≤2 → failed)"| analysis
    publish ==>|"failure"| terminal_failed
    provider ==>|"success"| terminal_shipped
    provider ==>|"no_change"| terminal_no_change
    provider ==>|"semantic_repair_required"| terminal_needs_human
    provider -.->|"retryable_infrastructure_failure (≤2 → failed)"| analysis
    provider ==>|"failure"| terminal_failed
    %% every stage also: needs_human (11), canceled (11), superseded (11)
    %%   needs_human -> terminal needs_human
    %%   canceled -> terminal canceled
    %%   superseded -> terminal superseded
```

### Legend

- Rectangle = agent stage, parallelogram = command gate, hexagon = provider wait.
- Solid arrow = forward transition; dashed arrow = re-entry (the target is the
  stage itself or sits at/before it in the declared stage order, matching the
  shared `isPipelineReentry` manifest helper); thick arrow = terminal outcome.
- `(≤N → outcome)` on an edge is `max_reentries` and its `on_exhausted` terminal.
- Outcomes identical across every stage are suppressed and listed in the
  trailing `%%` comment inside the diagram.
